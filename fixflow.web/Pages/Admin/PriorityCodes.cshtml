@page
@model fixflow.web.Pages.Admin.PriorityCodesModel
@{
    ViewData["Title"] = "Manage Priority Codes";
}

<div class="page-shell">
    <div class="page-header">
        <h1 class="page-title"><i data-lucide="alert-triangle"></i> Manage Priority Codes</h1>
        <p class="page-subtitle">Configure priority levels for ticket classification</p>
    </div>

    <div class="toolbar">
        <div></div>
        <a asp-page="./PriorityCodeCreate" class="btn btn-primary">
            <i data-lucide="plus-circle"></i> Add New Priority Code
        </a>
    </div>

    <div class="card">
        @if (Model.PriorityCodes != null && Model.PriorityCodes.Any())
        {
            <table>
                <thead>
                    <tr>
                        <th><i data-lucide="hash"></i> Code</th>
                        <th><i data-lucide="alert-triangle"></i> Priority Name</th>
                        <th><i data-lucide="edit"></i> Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var priority in Model.PriorityCodes)
                    {
                        <tr>
                            <td>@priority.Code</td>
                            <td>@priority.PriorityName</td>
                            <td>
                                <a href="#" class="btn btn-ghost"><i data-lucide="pencil"></i> Edit</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        {
            <div class="placeholder">
                <i data-lucide="alert-triangle" class="icon-lg icon-faded"></i>
                <p>No priority codes configured yet. Click "Add New Priority Code" to get started.</p>
            </div>
        }
    </div>
</div>