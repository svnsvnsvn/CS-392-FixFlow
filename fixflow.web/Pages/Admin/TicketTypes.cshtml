@page
@model fixflow.web.Pages.Admin.TicketTypesModel
@{
    ViewData["Title"] = "Manage Ticket Types";
}

<div class="page-shell">
    <div class="page-header">
        <h1 class="page-title"><i class="bi bi-folder"></i> Manage Ticket Types</h1>
        <p class="page-subtitle">Configure categories for maintenance requests</p>
    </div>

    <div class="toolbar">
        <a href="/Dashboard" class="btn btn-secondary"><i class="bi bi-arrow-left"></i> Back to Dashboard</a>
        <a asp-page="./TicketTypeCreate" class="btn btn-primary">
            <i class="bi bi-plus-circle"></i> Add New Ticket Type
        </a>
    </div>

    <div class="card">
        @if (Model.TicketTypes != null && Model.TicketTypes.Any())
        {
            <table>
                <thead>
                    <tr>
                        <th><i class="bi bi-hash"></i> Code</th>
                        <th><i class="bi bi-folder"></i> Type Name</th>
                        <th><i class="bi bi-pencil-square"></i> Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var ticketType in Model.TicketTypes)
                    {
                        <tr>
                            <td>@ticketType.Id</td>
                            <td>@ticketType.TypeName</td>
                            <td>
                                <a href="#" class="btn btn-ghost"><i class="bi bi-pencil"></i> Edit</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        {
            <div class="placeholder">
                <i class="bi bi-folder icon-lg icon-faded"></i>
                <p>No ticket types configured yet. Click "Add New Ticket Type" to get started.</p>
            </div>
        }
    </div>
</div>